#ifndef _INTEFACESH_#define _INTEFACESH_#include <cmath>#include <iostream>using namespace std;class IGeoFig {public:    virtual double area() const = 0;    virtual double perimeter() const= 0;};struct CVector2D {    double x, y;};double LINEAR_DENSITY = 1.; //линейная плотность для нахождения массы = линейная плотность * площадь// Интерфейс "Физический объектclass IPhysObject {public:    virtual double mass() const = 0;    virtual CVector2D position() const = 0;    virtual bool operator == (const IPhysObject& ob) const = 0;    virtual bool operator < (const IPhysObject& ob) const = 0;};// Интерфейс "Отображаемый"class IPrintable {public:    virtual void draw() const = 0;};// Интерфейс для классов, которые можно задать через диалог с пользователем.class IDialogInitiable {public:    virtual void InitFromDialog() = 0;};// Интерфейс "Класс"class BaseCObject {public:    virtual const char* classname() const = 0;    virtual unsigned int size() const = 0;};#endif